<UserControl x:Class="AcousticTransferMatrices.Core.CustomControls.PropertyGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AcousticTransferMatrices.Core.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="75*"/>
        </Grid.ColumnDefinitions>
        <StackPanel x:FieldModifier="private" x:Name="txtDiscription" Grid.Column="0" Margin="10,15,5,0">

        </StackPanel>
        <StackPanel x:Name="txtProperty" x:FieldModifier="private" Grid.Column="1" Margin="5,15,10,0">

        </StackPanel>

    </Grid>
    </ScrollViewer>
</UserControl>
